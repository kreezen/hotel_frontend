import { SortOrderDirective } from './sort-order.directive';

describe('SortOrderDirective', () => {
  it('should create an instance', () => {
    const directive = new SortOrderDirective();
    expect(directive).toBeTruthy();
  });
});
